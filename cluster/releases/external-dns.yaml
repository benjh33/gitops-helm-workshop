apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: external-dna
  namespace: external-dna
  annotations:
    fluxcd.io/ignore: "false"
spec:
  releaseName: external-dns
  chart:
    git: git@github.com:benjh33/gitops-helm-workshop
    ref: master
    path: cluster/charts/external-dns
  values:
